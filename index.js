/**
 * @format
 */

import { AppRegistry } from 'react-native';
import OrchwApp from './OrchwApp';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => OrchwApp);
